[project]
name = "richmode"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
hello = "richmode:hello"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
